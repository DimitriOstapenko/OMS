New order #<%= @order.id %> just received from client <%= @client.name %>

Ordered <%= @order.products.count %> <%= 'product'.pluralize(@order.products.count) %>

  <% @order.placements.each do |placement| %>
    <%= placement.product.ref_code %> <%= placement.product.description %> Price: <%= number_to_currency placement.product.price_eu %> Qty: <%= placement.quantity %>
  <% end %>

Order Total: <%= number_to_currency @order.total %>

<%= link_to 'Follow this link to see the order', orders_url(@client) %>

