<% if current_user %>
  <ul class="nav navbar-nav navbar-left">
    <li class="dropdown nav-item">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-menu-hamburger text-muted"></i></a>
      <ul class="dropdown-menu">
        <li><%= link_to "<i class='glyphicon glyphicon-user'></i> My Profile".html_safe, edit_user_registration_path(current_user.id) %></li>
      <% if current_user.admin? || current_user.staff? %>
        <li><%= link_to "<i class='glyphicon glyphicon-euro'></i> Price Models".html_safe, prices_path %></li>
        <li><%= link_to "<i class='glyphicon glyphicon-envelope'></i> Client Mailer".html_safe, client_mails_path %></li>
        <li><%= link_to "<i class='glyphicon glyphicon-export'></i> Export Orders".html_safe, export_path %></li>
        <li><%= link_to "<i class='glyphicon glyphicon-check'></i> Data Integrity Check".html_safe, self_test_path %></li>
      <% end %>
        <li class="divider"></li>
        <li><%= link_to "<i class='glyphicon glyphicon-log-out'></i> Logout".html_safe, destroy_user_session_path, method: :delete %></li>
      </ul>
    </li>
  </ul>
    <%= link_to "APD OMS", root_path, id: "logo" %>
    <% if current_user %>
      <span class="navbar-brand"> <%= current_user.name %>: <%= current_user.role %> </span>
    <% end %>
<% else %>  
    <%= link_to "APD ORDER MANAGEMENT SYSTEM", root_path, id: "logo" %>
<% end %>

    
