<b><%= collection.count == 0 ? 'Ok' : collection.count %></b> 
<% if collection.count > 0 %>
  <% if collection.count > 1 %>
     , including: 
  <% end %>
  <% collection[0..4].each do |cl| %>
    <%= link_to cl.code, edit_client_path(cl), remote: true, 'data-toggle':  "modal", 'data-target': '#modal-window' %>
  <% end %>
  <% if collection.count > 5 %>
  and <%= collection.count-5 %> more
  <% end %>
<% end %>

