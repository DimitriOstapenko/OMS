<b><%= collection.count == 0 ? 'Ok' : collection.count %></b> 
<% if collection.count > 0 %>
  :
  <% collection.each do |img| %>
    <%= link_to img, "/images/fullsize/#{img}.jpg" %> 
  <% end %>
<% end %>
