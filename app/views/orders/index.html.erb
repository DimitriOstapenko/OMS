<%= render "shared/modal" %>

<% if @orders.any? %>
<% first_page = params[:page] == '1' || params[:page].blank? %>
<p></p>
<%= render "totals_header" if first_page %>

<div style="float:right"><small>Cancelled orders are highlighted in red; orders by client in blue</small></div>
<% scroll = "table-wrapper-scroll-x" if mobile_device? %>
<table class="tbl2 table-hover table-striped table-bordered <%= scroll %>" >
<%= render "#{current_user.role}_header" %>
  
<tbody>
<%= render @orders, remote: device_is_desktop?  %>
</tbody>
</table>

<div class="row">
  <div class="col-md-6">
    <%= render "totals_table" if first_page %>
  </div>

  <div class="col-md-6">
  <div class="bottom_nav">
<%= will_paginate @orders, class: "btn" %>
  </div>
  </div>
</div>

<% end %>

<script>
$zoho.salesiq.ready=function(embedinfo)
{ $zoho.salesiq.floatbutton.visible("hide"); }
</script>

